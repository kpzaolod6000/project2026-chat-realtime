{
  "extends": "@chat/config/tsconfig/node.json",

  // Only path-valued options and package-specific overrides live here.
  // Everything else comes from the preset.
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
